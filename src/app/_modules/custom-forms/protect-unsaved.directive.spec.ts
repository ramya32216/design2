import { ProtectUnsavedDirective } from './protect-unsaved.directive';

describe('ProtectUnsavedDirective', () => {
  it('should create an instance', () => {
    const directive = new ProtectUnsavedDirective();
    expect(directive).toBeTruthy();
  });
});
