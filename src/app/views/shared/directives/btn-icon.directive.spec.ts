import { BtnIconDirective } from './btn-icon.directive';

describe('BtnIconDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnIconDirective();
    expect(directive).toBeTruthy();
  });
});
