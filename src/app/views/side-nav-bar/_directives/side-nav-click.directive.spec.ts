import { SideNavClickDirective } from './side-nav-click.directive';

describe('SideNavClickDirective', () => {
  it('should create an instance', () => {
    const directive = new SideNavClickDirective();
    expect(directive).toBeTruthy();
  });
});
